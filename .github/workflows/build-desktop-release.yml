name: Build-Desktop-Release

on:
  workflow_dispatch:
    inputs:
      build-target:
        description: 'Build Target (Release Type)'
        type: choice
        required: true
        options:
          - beta
          - nightly
          - non-release
        default: "non-release"
      git-ref:
        description: "Release Git Ref (Which branch or tag to build?)"
        required: true
        default: "master"
      is-draft:
        description: 'Draft Release? (Beta only, Nightly will always be a non-draft)'
        type: boolean
        required: true
        default: true
      is-pre-release:
        description: 'Pre Release? (labeled as "PreRelease")'
        type: boolean
        required: true
        default: true
      enable-file-sync-production:
        description: 'File sync production mode'
        type: boolean
        required: true
        default: true
      enable-plugins:
        description: 'Build with plugin system support'
        type: boolean
        required: true
        default: true
      build-android:
        description: 'Build Android App'
        type: boolean
        required: true
        default: true

env:
  CLOJURE_VERSION: '1.11.1.1413'
  NODE_VERSION: '20.11.1'
  JAVA_VERSION: '17'
  # تم حذف متغيرات Sentry من هنا

jobs:
  build-electron:
    name: Build Electron App (Linux x64 Only)
    # تم حذف المصفوفة (matrix) وحصر العمل في أوبنتو فقط
    runs-on: ubuntu-20.04
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.git-ref }}
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'yarn'

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'zulu'

      - name: Set up Clojure
        uses: DeLaGuardo/setup-clojure@12.1
        with:
          cli: ${{ env.CLOJURE_VERSION }}

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build Logseq App
        env:
          # تم حذف متغيرات Sentry
          start_of_week: :monday
          LOGSEQ_CI: true
          ENABLE_FILE_SYNC: ${{ inputs.enable-file-sync-production }}
          ENABLE_PLUGINS: ${{ inputs.enable-plugins }}
        run: |
          # هذا الأمر سيبني نسخة لينكس الافتراضية (عادة x64 على هذا النظام)
          yarn release-electron

      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: logseq-linux-builds
          path: |
            static/out/*
            !static/out/*-unpacked

  build-android:
    if: ${{ inputs.build-android == 'true' }}
    name: Build Android App (Unsigned/Debug)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.git-ref }}
          submodules: true

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'zulu'
          cache: 'gradle'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build Android APK
        env:
          LOGSEQ_CI: true
          ENABLE_FILE_SYNC: ${{ inputs.enable-file-sync-production }}
          ENABLE_PLUGINS: ${{ inputs.enable-plugins }}
        # تم تغيير الأمر إلى assembleDebug لتجنب مشاكل التوقيع
        run: |
          cd ./src/main/frontend/
          yarn install
          npx shadow-cljs release app
          cd ../../../
          ./gradlew clean :app:assembleDebug --stacktrace

      - name: Upload Android Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: logseq-android-builds
          # المسار الجديد لملفات الـ Debug
          path: ./android/app/build/outputs/apk/debug/*.apk

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [build-electron, build-android]
    if: ${{ !cancelled() }} 
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download Linux Artifacts
        uses: actions/download-artifact@v3
        with:
          name: logseq-linux-builds
          path: ./

      - name: Download Android Artifacts
        if: ${{ inputs.build-android == 'true' }}
        uses: actions/download-artifact@v3
        with:
          name: logseq-android-builds
          path: ./

      - name: Retrieve tag version
        id: ref
        run: |
          # محاولة قراءة الإصدار، إذا لم يوجد الملف نضع قيمة افتراضية
          if [ -f VERSION ]; then
            pkgver=$(cat VERSION)
          else
            pkgver="custom-build"
          fi
          echo "version=$pkgver" >> $GITHUB_OUTPUT

      # تم حذف خطوة إصلاح أسماء ملفات ويندوز (Fix .nupkg) لأننا لم نعد نبنيها

      - name: Generate SHA256 checksums
        run: |
          # نقوم بحساب المجموع الاختباري لملفات لينكس وأندرويد فقط
          # استخدام || true لمنع الفشل في حال عدم وجود ملفات
          sha256sum *-linux-* > SHA256SUMS.txt || true
          sha256sum *.apk >> SHA256SUMS.txt || true
          cat SHA256SUMS.txt

      - name: Create Release Draft
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.ref.outputs.version }}
          name: Logseq Custom Build ${{ steps.ref.outputs.version }}
          body: "Custom build: Linux x64 and Android (Unsigned/Debug)."
          draft: ${{ inputs.is-draft }}
          prerelease: ${{ inputs.is-pre-release }}
          files: |
            *.AppImage
            *.deb
            *.rpm
            *.apk
            SHA256SUMS.txt
